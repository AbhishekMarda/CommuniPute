{
  "name": "convex-tutorial",
  "version": "0.0.0",
  "scripts": {
    "dev": "npm-run-all init dev:init --parallel dev:server",
    "build": "vite build",
    "init": "convex init",
    "dev:server": "convex dev",
    "dev:init": "convex dev --once"
  },
  "dependencies": {
    "convex": "0.9.1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
